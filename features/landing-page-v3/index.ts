/**
 * Landing Page V3 — Spatial + Motion (2026)
 *
 * Editorial / cinematic portfolio with:
 * - Custom cursor with multiple states
 * - Canvas shader background (mimics WebGL fluid)
 * - Iridescent conic gradients & accent text
 * - Tilt cards, drag-scroll catalog
 * - Velocity-aware marquee tapes
 * - Floating section nav
 * - Cinematic intro loader
 *
 * Tokens scoped to .v3-root — no collision with v1/v2.
 */
export { LandingPageV3 } from './views/landing-page-v3'
export * from './constants'
